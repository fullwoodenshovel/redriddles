This is a pixel art editor made from scratch (so, ability to parse user inputs and basic screen drawing operations) for a friend.

It will have the ability to read texture folders and translate from pixel art to those textures, but currently cannot do this.
